Se utiliza para ejecutar un contenedor de forma **interactiva** y con una **terminal asignada**

```bash
docker run -it <imagen> <comando>
```

## Ejemplo

En este caso entrarias al mismo contenedor que el de **Ubuntu**

```bash
docker run -it ubuntu bash
```

## Tags

###### #Docker
