Puede utilizar el método de `string` para recuperar los datos de la solicitud como una instancia de `Illuminate\Support\Stringable`

```php
$name = $request->string('name')->trim();
```
## Tags

##### #Laravel
##### #Laravel-Request