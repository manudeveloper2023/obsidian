Si necesitas determinar si la vista exista , puedes utilizar un `View` , puedes usar `exists` , para verificar si la vista existe

```php
use Illuminate\Support\Facades\View;
 
if (View::exists('admin.profile')) {
    // ...
}
```

## Tags

##### #Laravel
##### #Laravel-View