Puedes usar la fachada de `View` , puedes utilizar el método de `View` para veríficar el primer arreglo de `views`

## Ejemplo

```php
use Illuminate\Support\Facades\View;
 
return View::first(['custom.admin', 'admin'], $data);
```

## Tags

##### #Laravel
##### #Laravel-View
